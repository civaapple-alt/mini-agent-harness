---
name: context7-mcp
description: Fetch current, version-specific library documentation with Context7 when a task depends on a third-party library, framework, SDK, API, or CLI.
---

# Context7 MCP

Resolve the requested library first, then query its documentation for one
specific topic at a time. Prefer official packages and version-specific results.
